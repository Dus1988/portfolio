export enum BootstrapColumns {
    Full = 12,
    ThreeQuarter = 9,
    TwoThird = 8,
    Half = 6,
    Third = 4,
    Quarter = 3
}

export enum CardTypes {
    Default = "",
    NoPadding = "no-padding",
    Alert = "alert",
    Gradient = "gradient",
    Handout = "handout" 
}