import { FutureDateCheckDirective } from './future-date-check.directive';

describe('FutureDateCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new FutureDateCheckDirective();
    expect(directive).toBeTruthy();
  });
});
