import { CheckOverflowDirective } from './check-overflow.directive';

describe('CheckOverflowDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckOverflowDirective();
    expect(directive).toBeTruthy();
  });
});
